[project]
name = "pr-review-bot"
version = "0.1.0"
description = "PR Review Slack Notification Bot for fractalyze org"
requires-python = ">=3.11"
dependencies = [
    "slack-sdk>=3.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.3.0",
]
